{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
  <div class="row g-3">
    <div class="col-md-6">
      {{ form_row(form.firstName) }}
    </div>
    <div class="col-md-6">
      {{ form_row(form.lastName) }}
    </div>
    <div class="col-md-12">
      {{ form_row(form.email) }}
    </div>
    <div class="col-md-12">
      {{ form_row(form.cv) }}
    </div>
    {# Ajoute ici d'autres champs comme mot de passe si besoin #}
  </div>

  <div class="mt-4">
    <button class="btn btn-primary w-100">
      <i class="bi bi-person-plus"></i> Créer mon compte
    </button>
  </div>
{{ form_end(form) }}
